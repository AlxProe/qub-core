# QUB Explorer assets

This HF117 source package is **no-assets**.

Before Explorer deploy, keep or add the production `/assets` files on the server, including the v0.6 Matrixchain/JIN twins icons:

- enj.png
- enj-white.png
- jin-token.png
- jin-token-white.png

Do not deploy this placeholder folder with `rsync --delete` over production assets.
